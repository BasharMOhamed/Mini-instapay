apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels:
    app: user
spec:
  selector:
    app: user
  ports:
    - port: 5000
      targetPort: http
      protocol: TCP
  type: NodePort
