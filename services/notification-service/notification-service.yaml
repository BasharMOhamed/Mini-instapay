apiVersion: v1
kind: Service
metadata:
  name: notification-service
  labels:
    app: notification
spec:
  selector:
    app: notification
  ports:
    - port: 5004
      targetPort: http
      protocol: TCP
  type: NodePort
